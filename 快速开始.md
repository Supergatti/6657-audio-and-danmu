# 🚀 快速开始 - 弹幕飘过服务器

## 一、启动服务器

打开PowerShell或命令提示符，运行：

```powershell
python douyu_danmaku.py
```

你会看到类似这样的输出：
```
[DEBUG 14:30:25] room_id=6979222
[DEBUG 14:30:26] 房间信息 - 主播: XXX, 标题: XXX
[DEBUG 14:30:27] 斗鱼WebSocket已连接: wss://...
[DEBUG 14:30:27] 官方页面已加载
[DEBUG 14:30:27] 弹幕飘过服务器启动 - http://127.0.0.1:8766
[DEBUG 14:30:27] 在浏览器中打开: http://127.0.0.1:8766
```

## 二、打开弹幕墙

在浏览器中访问：**http://127.0.0.1:8766**

就能看到弹幕从右向左飘过了！🎊

## 三、自定义选项

### 监听其他房间
```powershell
python douyu_danmaku.py --room-url https://www.douyu.com/你的房间号
```

### 修改端口
```powershell
python douyu_danmaku.py --port 9999
```
然后访问：http://127.0.0.1:9999

### 调整缓存大小（默认500条）
```powershell
python douyu_danmaku.py --max-cache 1000
```

### 显示浏览器窗口（调试用）
```powershell
python douyu_danmaku.py --headed
```

## 四、常用组合

```powershell
# 监听特定房间，使用8888端口，缓存1000条
python douyu_danmaku.py --room-url https://www.douyu.com/178432 --port 8888 --max-cache 1000
```

## 五、停止服务

按 `Ctrl+C` 即可停止服务器。

## 六、特点说明

✅ **不保存日志** - 弹幕仅在内存中，每500条自动清空
✅ **实时显示** - WebSocket推送，延迟极低
✅ **多轨道** - 12条轨道智能分配，弹幕不重叠
✅ **可调控制** - 速度、字体、透明度都可以在页面上调节

---

**详细说明**：查看 README_弹幕飘过.md
